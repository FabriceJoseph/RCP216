services:
  rcp216:
    build:
      context: .
      dockerfile: Dockerfile
    image: rcp216:latest
    working_dir: /workspace
    volumes:
      - .:/workspace
    ports:
      - "8888:8888"
    stdin_open: true
    tty: true
